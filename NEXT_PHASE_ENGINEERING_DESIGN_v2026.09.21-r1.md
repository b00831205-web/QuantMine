# 下一阶段工程设计修订：同日不可变市场数据修订版

> 文档版本：`v2026.09.21-r1`  
> 状态：对 `NEXT_PHASE_ENGINEERING_DESIGN_v2026.09.21.md` 的追加设计  
> 基线：原 `v2026.09.21` 文件保持不可变，不作修改。

## 1. 决策

当覆盖率审计发现已发布市场数据版本存在历史缺口，系统采用**同日修订版**发布策略：

```text
基础版：20260921
首个修订版：20260921-r1
第二个修订版：20260921-r2
```

基础版和每个修订版均为不可变目录。回填不得重写或删除既有版本。

## 2. 版本选择规则

市场数据版本的排序键为：

```text
(交易日期, 修订号)
```

- 没有修订后缀的基础版修订号为 `0`。
- 对同一个交易日期，`-r2` 高于 `-r1`，`-r1` 高于基础版。
- “截至某交易日的最新版本”选择日期不晚于该日、且排序键最大的完整版本。
- 所有 API、研究绑定和前端数据日期必须使用该解析结果，不能假设版本总是八位日期字符串。

## 3. 回填发布流程

```text
daily market-data refresh
  → coverage_audit
  → persisted backfill plan
  → history_backfill workers
  → staged repair artifacts
  → repair publication
  → YYYYMMDD-rN immutable version
  → readiness gate
  → research / IC / backtest
```

1. 日增量先发布基础版 `YYYYMMDD`，不被历史回填阻塞。
2. 覆盖审计从交易日历、证券主数据和基础版字段矩阵自动派生缺口；调用方不得传入固定回填日期。
3. 历史 worker 仅消费持久化计划中的日期窗口、证券和字段，结果写入可续跑暂存区。
4. 修订发布阶段读取基础版和已完成暂存任务，按“基础版非空值优先、暂存数据仅填补空值”的规则合并。
5. 合并结果写入下一个可用修订号 `YYYYMMDD-rN`；若内容没有变化，则不创建新修订版。
6. 审计、计划、暂存任务、基础版本和修订版本的关联必须保存在 manifest 中。

## 4. 数据合并约束

- 回填数据不得覆盖基础版中已存在的非空数值。
- 回填字段只能补齐对应字段的空单元格。
- 交易日依据市场参考日历，不得使用工作日历推断 A 股交易日。
- 每个回填任务需保留 `plan_id`、`checkpoint_key`、日期范围、证券、字段和内容哈希。
- 修订版 manifest 必须记录 `base_version`、`revision`、已应用任务及内容哈希。

## 5. 读取与门禁

- `research_readiness_gate` 只允许消费满足覆盖率策略的最新修订版。
- 基础版不完整且修订版尚未完成时，研究、IC 和回测必须 skip。
- 前端的 Data Date、版本号、覆盖率和回填状态均来自解析后的最新可用版本。

## 6. 迁移范围

需要修改或新增：

- `quantmine/workflows/market_data_publication.py`：解析和选择 `YYYYMMDD-rN` 版本。
- 市场数据读取绑定：从固定 `{as_of_date}` 路径改为“截至日期的最新版本”解析。
- 修订发布 worker：从基础版与暂存回填结果发布新的不可变修订版。
- `research_readiness_gate`：读取覆盖审计及修订版本状态。
- 市场 API 与前端版本显示：返回并展示完整版本标识。

## 7. 验收标准

1. 基础版 `YYYYMMDD` 发布后，历史缺口回填可创建 `YYYYMMDD-r1`，且基础版内容不变。
2. 最新版本解析在同日基础版、`-r1`、`-r2` 并存时选择 `-r2`。
3. 未完成回填不生成伪完整版本；下游研究任务被 readiness gate 跳过。
4. 暂存任务重跑不重复下载，修订发布重跑不产生不同内容的同名版本。
5. 前端与 API 显示解析后的最新修订版本及其覆盖率和回填状态。
